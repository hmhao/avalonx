import { Store } from './store'
import { mapState, mapMutations, mapGetters, mapActions } from './helpers'

export default {
  Store,
  version: '__VERSION__',
  mapState,
  mapMutations,
  mapGetters,
  mapActions
}
